import React from "react";

export default class JoinAGroup extends React.Component {
    render() {
        return (
            <div>
                <h1>Enter url to join a room.</h1>
            </div>
        );
    }
}